{
  "name": "waves-n-wishes-concierge",
  "version": "1.0.0",
  "description": "AI Concierge for Waves N Wishes",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && esbuild index.tsx --bundle --outfile=public/bundle.js --loader:.tsx=tsx --platform=browser --target=es2020 --define:process.env.API_KEY='\"'\"$API_KEY\"'\"'"
  },
  "dependencies": {
    "@google/genai": "^1.28.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}